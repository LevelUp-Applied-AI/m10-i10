# `web/` is pre-implemented in the live template repo

> **Step 9c materialization gap — not yet closed.** Same pattern as
> `api/PLACEHOLDER.md`. At Step 9c, the Lab's pre-implemented Next.js
> frontend is supposed to be copied into this directory before the
> integration template repo is pushed; the Lab reference frontend
> (typed `fetch` against `/extract`, `/kg/query`, `/rag/answer`,
> citation-marker rendering, error handling) does not yet exist.
> Tracked as a Round-3 carry-forward; the 9c-vi gate blocks publication
> of learner-facing template-repo URLs until this is closed.
