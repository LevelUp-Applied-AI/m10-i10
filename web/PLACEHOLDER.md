# `web/` is pre-implemented in the live template repo

Same pattern as `api/PLACEHOLDER.md`. At Step 9c, the Lab's
pre-implemented Next.js frontend is copied into this directory before
the integration template repo is pushed.
